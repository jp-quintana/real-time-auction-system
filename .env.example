# Used by docker-compose for ${VAR} interpolation in docker-compose.yml.
# Copy to .env and fill in.

DB_USER=user
DB_PASSWORD=password
DB_NAME=mydb

# Host-side ports (containers always use defaults internally).
DB_PORT=5433
REDIS_CACHE_PORT=6380
REDIS_QUEUE_PORT=6381
REDIS_INSIGHT_PORT=5541